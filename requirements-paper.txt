# Pinned environment used to produce the OCEANS 2026 paper results.
# Tested with Python 3.11.9.
# For a general-purpose install use requirements.txt instead.

# Core numerical stack
numpy==2.4.4
matplotlib

# Environment interface
gymnasium==1.3.0
pettingzoo

# Deep learning backend
torch==2.10.0

# Reinforcement Learning
stable-baselines3==2.8.0

# Logging / visualization
tensorboard
pandas
tqdm
rich

# LLM providers (required only for --llm evaluation)
langchain-openai
langchain-core
openai
anthropic
